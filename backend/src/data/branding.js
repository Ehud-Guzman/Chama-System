// The group's name, for documents that are rendered before Settings has been read
// (or when it has never been filled in). Everything that can read the settings uses
// `settings.chamaName` instead — this is the floor, not the source.
const CHAMA_NAME = 'WAZO MOJA SELF-HELP GROUP';

module.exports = { CHAMA_NAME };
