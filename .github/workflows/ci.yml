# Continuous integration.
#
# Three things are checked on every push, in this order of importance:
#
#   1. Member data must not be in the repository. This is the check that would have
#      caught the spreadsheets (see the README, "Member data in this repository").
#   2. The test suite passes — the week engine, money arithmetic, upload screening,
#      the members' gate's credentials, and the API's own behaviour.
#   3. The frontend builds.
#
# No secrets are needed: nothing here talks to MongoDB, Cloudinary or SMTP.
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  backend:
    name: Backend (data check, tests, audit)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    services:
      # The rehearsal suite drives the real money paths against a real MongoDB: log a
      # payment, issue and settle a fine, refuse a save that cuts the members' total, then
      # take a backup through the API and restore it into a second database. It is worth a
      # database in CI because that is where a backup-format or casting bug shows up.
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install
        run: npm ci

      - name: Member data must not be tracked
        run: node scripts/checkNoDataFiles.mjs

      - name: Tests
        run: npm test

      - name: Rehearsal against a real MongoDB
        run: npm run test:integration

      - name: Dependencies (runtime)
        run: npm audit --omit=dev --audit-level=high
        continue-on-error: true

  frontend:
    name: Frontend (build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build
